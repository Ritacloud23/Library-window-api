[project]
name = "windows-storage"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ritacloud23", email = "rnnanne53@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "uvicorn>=0.52.4",
]

[project.scripts]
windows-storage = "windows_storage:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
